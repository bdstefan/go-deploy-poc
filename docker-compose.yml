version: "3.3"
services:
  app:
    container_name: "gopoc"
    build: "./app"
    environment:
      - REDIS_HOST=redis
    ports:
      - "3030:3030"
    networks:
      - redis-net
    depends_on:
      - redis

  redis:
    image: redis:4.0.5-alpine
    command: ["redis-server", "--appendonly", "yes"]
    hostname: redis
    networks:
      - redis-net
    volumes:
      - redis-data:/data

networks:
  redis-net:

volumes:
  redis-data: